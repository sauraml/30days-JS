// • Task 4: Write a function expression to concatenate two strings and return the result.

const concatStrings = function (str1, str2) {
    return str1 + str2;
};

const s = "Hello";
const p = "Javascript";
const sayHello = concatStrings(s, p);

console.log(sayHello);
